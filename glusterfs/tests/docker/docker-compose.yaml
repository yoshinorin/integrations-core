services:
  gs1:
    container_name: gluster-node-1
    image: gluster/gluster-centos:latest
    build: ./
    privileged: true
    volumes:
      - ./gluster_run:/var/run/
    
  gs2:
    container_name: gluster-node-2
    image: gluster/gluster-centos:latest
    build: ./
    privileged: true
